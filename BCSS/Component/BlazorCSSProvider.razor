@namespace BCSS
@inherits ComponentBase
@inject BCSSService BCSSService

<style>
    @foreach (var item in _keyValuePairs)
    {
        var processedValue = item.Value?.Split(' ') ?? new string[0];
        @($".{item.Key} {{ {string.Join("!important;", processedValue) + "!important;"} }}")
    }
</style>

